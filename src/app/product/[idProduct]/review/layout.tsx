const ProductReview = ({
    children,
}: {
    children: React.ReactNode
}) => {
    return (
        <>
            {children}
            <div>
                lay out review
            </div>
        </>

    );
}

export default ProductReview;